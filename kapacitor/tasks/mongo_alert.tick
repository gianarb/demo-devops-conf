stream
    |from()
        .database('get_metr')
        .retentionPolicy('default')
        .measurement('mongodb')
    |alert()
        .crit(lambda: "inserts_per_sec" > 70)
        .log('/tmp/alerts.log')
